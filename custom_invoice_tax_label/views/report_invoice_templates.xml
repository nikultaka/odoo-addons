<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!--
        Odoo 19: the customer-invoice PDF title is rendered from
            <t t-set="layout_document_title"> ... </t>
        inside account.report_invoice_document. Both the regular print path
        (proforma == False) and the "Preview" path (proforma == True) live in
        the SAME template, switched by the `proforma` context variable.

        We replace the entire t-set block so that, for move_type == 'out_invoice',
        the title is always "Tax Invoice" regardless of state (draft / posted /
        cancel) and regardless of proforma vs print. Other move types keep
        their original labels.
    -->
    <template id="report_invoice_document_inherit_tax_label"
              inherit_id="account.report_invoice_document"
              priority="100">
        <xpath expr="//t[@t-set='layout_document_title']" position="replace">
            <t t-set="layout_document_title">
                <t t-if="o.move_type == 'out_invoice'">
                    <span><t name="tax_invoice_title">Tax Invoice</t></span>
                </t>
                <t t-elif="not proforma">
                    <span t-if="o.move_type == 'out_refund' and o.state == 'posted'">
                        <t name="credit_note_title">Credit Note</t>
                    </span>
                    <span t-elif="o.move_type == 'out_refund' and o.state == 'draft'">
                        <t name="draft_credit_note_title">Draft Credit Note</t>
                    </span>
                    <span t-elif="o.move_type == 'out_refund' and o.state == 'cancel'">
                        <t name="cancelled_credit_note_title">Cancelled Credit Note</t>
                    </span>
                    <span t-elif="o.move_type == 'in_refund' and not o.journal_id.is_self_billing">
                        <t name="vendor_credit_note_title">Vendor Credit Note</t>
                    </span>
                    <span t-elif="o.move_type == 'in_refund' and o.journal_id.is_self_billing">
                        <t name="self_billing_credit_note_title">Self Billing Credit Note</t>
                    </span>
                    <span t-elif="o.move_type == 'in_invoice' and not o.journal_id.is_self_billing">
                        <t name="vendor_bill_title">Vendor Bill</t>
                    </span>
                    <span t-elif="o.move_type == 'in_invoice' and o.journal_id.is_self_billing">
                        <t name="self_billing_invoice_title">Self Billing</t>
                    </span>
                </t>
                <t t-else="">
                    <span t-if="o.move_type == 'out_refund' and o.state == 'posted'">
                        <t name="proforma_credit_note_title">Proforma Credit Note</t>
                    </span>
                    <span t-elif="o.move_type == 'out_refund' and o.state == 'draft'">
                        <t name="draft_proforma_credit_note_title">Draft Proforma Credit Note</t>
                    </span>
                    <span t-elif="o.move_type == 'out_refund' and o.state == 'cancel'">
                        <t name="cancelled_proforma_credit_note_title">Cancelled Proforma Credit Note</t>
                    </span>
                    <span t-elif="o.move_type == 'in_refund'">
                        <t name="proforma_vendor_credit_note_title">Proforma Vendor Credit Note</t>
                    </span>
                    <span t-elif="o.move_type == 'in_invoice'">
                        <t name="proforma_vendor_bill_title">Proforma Vendor Bill</t>
                    </span>
                </t>
                <span t-if="o.name and o.name != '/'" t-field="o.name">INV/2026/0001</span>
            </t>
        </xpath>

        <!--
            Force Invoice Date and Due Date on the customer-invoice PDF to the
            DD-MM-YYYY format (e.g. 12-05-2026 for 12 May 2026), replacing the
            default locale-driven rendering of t-field on a Date field.
        -->
        <xpath expr="//div[@name='invoice_date']/div[@t-field='o.invoice_date']" position="replace">
            <div t-out="o.invoice_date and o.invoice_date.strftime('%d-%m-%Y') or ''">12-05-2026</div>
        </xpath>

        <xpath expr="//div[@name='due_date']/div[@t-field='o.invoice_date_due']" position="replace">
            <div t-out="o.invoice_date_due and o.invoice_date_due.strftime('%d-%m-%Y') or ''">12-05-2026</div>
        </xpath>
    </template>

</odoo>
